const initial_state = {
    name: 'indorse-frontend-challenge'
};

const Reducer = (state  = initial_state, action) => {
    switch (action.type) {
        default:
            return state;
    }
};


export default Reducer;